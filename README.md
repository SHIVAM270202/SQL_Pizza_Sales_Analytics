# SQL_Pizza_Sales_Analytics
This project analyzes pizza sales data using SQL to identify revenue trends, top-selling products, customer ordering patterns, and category performance. Using queries like grouping, filtering, aggregation, and date extraction, the raw data is converted into meaningful business insights that help in decision-making.
